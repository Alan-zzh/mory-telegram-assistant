# 个人关联频道广告与一小时重复刷屏修复回执

## 失败证据

- 生产用户 `8704705115` 的消息快照 `66204/66208` 均为“一日4Q”，初始 `is_ad=0/deleted=0`；其 Bio 和显示名无直接广告字样。
- Telegram `personal_chat=-1003735080194`，标题包含“换资车队有码就要”；最近帖子 caption 包含“微信支付宝有码就要、无风险、日赚、客服、私信、公群”等独立锚点。
- 生产用户 `8705446609` 在 42 分钟内四次发送“进群演个二十”（`65805/65807/65809/65816`），均未标广告、未删除，机器人继续回复。旧连续检测窗口仅 15 分钟。

## 修复

- `detect_profile_ad_signal()` 在个人频道存在时额外读取最近 3 条帖子正文/caption；入群审核与验证码解限复审均复用同一入口。
- 增加资金码盘、灰产组织、收益承诺、资料导流组合锚点；“换资 + 车队 + 有码/码商”标题作为高特异降级门禁。
- 重复检测优先读取 `message_snapshots`，以 60 分钟、至少 3 次为阈值，支持空格/标点/全半角规避及较长文本 94% 极近变体。
- 重复行为只按刷屏删除该组消息并在 AI 前终止，不写广告真值、不禁言、不写黑名单；签到、谢谢、在吗、666、两次重复、超窗和不同正常问题均放行。

## 验证

- 目标链：328 passed。
- 全仓：1044 passed；compileall、flake8、mypy、`verify_db_methods.py`（199 个委托方法）通过。
- 真实 Telegram 资料探针：命中 `source=personal_chat`，六类锚点成立。
- 真实四消息样本：新算法返回 `behavior_only=true/action=delete_repeat_only`，聚类 `65805/65807/65809/65816`。
- 生产现存消息收口：六条相关消息再次删除均返回 `already_absent`；重复用户 `blacklist/global_blacklist/mute_records` 前后均为 0。
- 明确广告账号：Telegram `restricted`，`blacklist/global_blacklist/mute_records` 均为 1。

## 发布状态与阻断

- 代码提交：`1cde29f`。
- 当前生产仍为 `v5.38.38`，双服务 active、NRestarts=0、health 200；这些仅证明当前运行态存活。
- **门禁阻断**：原工作区已有 30 项不属于本任务的 v5.38.39 巡检/规则/部署改动；`check_deploy_ready.py` 因工作树不干净退出 1。未覆盖、暂存、提交或部署这些既有改动。
- 因此本回执只证明代码、本地回归和当前事故消息已收口，不证明未来消息已由生产自动拦截。
