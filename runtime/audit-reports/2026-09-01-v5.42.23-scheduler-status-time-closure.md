# v5.42.23 调度状态时间生产闭环

## 结论

- v5.42.23 已从提交 `765a290e0cf5` 部署到 `/home/ubuntu/mory_assistant`。
- 调度成功、失败和漏调现在分别记录真实的 `last_status_at`；首次发生 `MISSED` 不再因 `last_run=0` 被最近一小时门禁漏掉。
- 累计失败证据新增具名结构，明确区分当前状态、累计失败/漏调次数、状态事件时间和历史错误原因；旧字符串继续兼容。

## 修复与复审

- 第一轮独立复审发现无列名累计字符串仍易误读，推动结构化输出。
- 第二轮复审发现 `MISSED` 没有事件时间且 `historical_*` 命名不准确，最终新增中央 schema 和 Alembic 0012，并改为 `cumulative_*`。
- 第三轮复审发现部署门禁仍使用旧 `last_run`，最终与生产巡检统一改为 `last_status_at`；全部 finding 已关闭。

## 验证

- 聚焦测试 107 passed；全仓单元测试 1475 passed；正式 Ruff、mypy、配置同步、DB 方法219项、文档一致性、py_compile 与 diff check 通过。
- Alembic 0012 通过重复升级、降级和再次升级；旧 `MISSED` 不用 `synced_at` 伪造事件时间。
- 8个关键生产文件 SHA-256 与本地一致；生产 `last_status_at` 列存在，未知当前状态时间0，最近一小时当前 error/missed 为0。
- production-truth 17/17、drift 7/7，均为0 gap、0 failed；双服务 active/enabled、NRestarts=0、health 200、启动日志干净、数据库完整。

## 自然业务证据与项目边界

- 09:20 TTL 自然执行后 success 725→726，fail保持1、miss保持0，旧失败未复发。
- 部署后自然群消息已核对为真人成员 `is_bot=false`，没有发送探针消息冒充流量。
- MoryFansBot 的 Bot/Web 进程在线且9919返回302；MediaOps三个主进程在线，8318与8819返回200。
- TokenLab/TokenPass六个单元保持inactive+masked；Docker/containerd保持inactive+disabled。

## 证据边界

- `burn_orphan` 在本次部署后的首个自然执行点为12:00，仍由持续 Goal 等待，不提前制造成功回执。
- 历史错误文本保留用于事故追踪，不清零；结构化字段明确标记为 `historical`，不再冒充当前故障。

## 部署三选一

**已部署**：v5.42.23 已完成版本、哈希、迁移、数据库、双服务、调度结构化证据、保留项目和停用边界验收。
