# v5.42.22 配置真相生产闭环

## 结论

- 2026-09-01 08:55 CST 部署提交 `87676ca5a0dc`，远端版本读回 `v5.42.22`。
- 17 个无消费者设置已从自然语言、Dashboard 和 API 入口移除；连同既有退役项，生产共 28 个退役顶层键全部不存在。
- 4 个传统文化播报兼容别名只负责路由到 `MYSTIC_BROADCAST_CONFIG`，展示、写入、落盘和部署均不再生成伪顶层键。
- 独立复审发现并阻止了“防撤回”映射到另一个无消费者开关的假成功；最终版本不保留该假入口。

## 本地门禁

- 聚焦回归 86 passed；全仓单元测试 1466 passed；独立复审 P1/P2 均关闭。
- `compileall`、正式 CI Ruff、正式 CI mypy、DB 方法 219/219、配置同步 a/b/c/d、文档一致性与 diff check 通过。
- records-autopilot apply/verify strict 同指纹通过；部署前 `check_deploy_ready.py` 全绿且工作树干净。

## 部署与回滚

- 哈希预检 345 个文件，上传 13 个差异文件、跳过 332 个相同文件；依赖锁一致，无 schema 变更。
- 代码、配置和数据库备份均为 0600；代码备份 SHA-256 为 `59ddbf2fc79d...e70c9f89c1`，数据库备份 SHA-256 为 `4331b34a35aa...265572f8`。
- 回滚使用生产闭环 JSON 中已固化的精确代码/配置备份命令；本次没有触发回滚。

## 生产验收

- 10 个受影响文件逐个 SHA-256 与本地一致；生产配置直接读回 `invalid_present=[]`，即 28 个退役键和 4 个伪顶层键均为 0 残留。
- `mory-assistant` 与 `mory-dashboard` 均 active/running/enabled、`NRestarts=0`，health 200，启动日志无错误，数据库完整。
- 项目巡检 production-truth 17/17、drift 7/7，均为 0 evidence gap、0 failed；配置、凭据、调度注册和只读业务回执通过。
- MoryFansBot 的 Bot/Web 进程在线且 9919 返回 302；MediaOps-COO 三个主进程和 API 服务在线，8318 与 8819 均返回 200。
- TokenLab/TokenPass 六个单元保持 inactive+masked，真实进程扫描为 none；Docker/containerd 保持 inactive+disabled，failed units 为 0。
- 服务器可用内存 6102 MiB，根盘使用 56%，最近一小时 OOM 为 0；没有停止任何保留项目组件。

## 证据边界

- 业务回执为生产现有传统文化任务和保留项目入口的只读证据；本轮没有制造 Telegram 用户消息冒充自然流量。
- 12:00 后 `burn_orphan` 首次自然执行及首个自然 Telegram 事件仍属于持续 Goal 后续观察，不影响本次配置清理发布成立。

## 部署三选一

**已部署**：v5.42.22 已在既定生产目标上线，并完成版本、哈希、配置、双服务、数据库、保留项目和非保留自启边界验收。
