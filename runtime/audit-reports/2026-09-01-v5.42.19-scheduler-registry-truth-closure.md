# v5.42.19 调度注册真相生产闭环

## 结论

- 2026-09-01 07:40 CST 部署提交 `7a1ad8bf108c`；远端版本 `v5.42.19`。
- 调度巡检不再信任任意日志关键词，只解析 JSON 并认证 `logger/function/level`；普通 Telegram 消息无法伪造成功或失败。
- 启动与热重载先落 `SCHEDULER_RECONCILE_BEGIN`，只有 BaseTask、场景触发器及启动/刷新全部完成后才落最终 `OK`；只有 begin、后续失败或回滚失败均阻断。

## 发现与纠正

- 初始补丁只扫描 BaseTask 失败，场景触发器和回滚错误可能被最后 120 行日志挤掉。
- 第一轮修正仍允许用户消息伪造关键词，且 BaseTask ready 早于场景刷新完成，存在短暂或挂起假绿。
- 第二轮修正把首次启动 begin-only 误分为 evidence gap；最终改为明确 failed。
- 独立专家三轮反向复审后最终 `PASS`，无 P0/P1/P2。

## 本地门禁

- 聚焦回归 66 passed；全仓单元测试 1454 passed。
- `compileall`、Ruff、mypy、DB 方法注册 219 项、配置同步、文档一致性、diff check 全部通过。
- records-autopilot apply/verify strict 同指纹通过；病历活动摘要保持 50 条，移出的旧条目仍由 Git 保存。

## 部署与回滚

- 部署器哈希预检 345 个文件，只上传 7 个差异文件；338 个相同文件跳过。
- 代码备份：`backups/code_deploy_1788219612.tar.gz`，SHA-256 `4738e5894737dcd3399a30d9f24b7c4a081719f8031779446eed607d2dcac24a`。
- 数据库备份：`backups/mory_pre_migration_1788219643_a9a350184cbf97d6.db`，部署器确认在线一致性快照。
- 回滚：恢复上述代码 tar 后重启 `mory-assistant` 与 `mory-dashboard`；数据库如需回滚，先停写再使用已校验快照。

## 生产验收

- 远端 `core/deploy_utils.py`、`core/bot_initializer.py`、`tasks/task_scheduler.py` SHA-256 与本地一致。
- Bot 与 Dashboard 均 `active/running/enabled`、`NRestarts=0`，health 200；启动窗口 ERROR/CRITICAL 为 0。
- 数据库 `integrity_check=ok`、外键违规 0；生产只读巡检 17 pass、0 evidence gap、0 failed。
- 真实启动日志：`BEGIN mode=startup generation=1` 后 32 ms 落 `OK`，`managed_jobs=41`、`scene_triggers=reconciled`、`dynamic_jobs=not_observed`。
- 重启后 07:44:48–07:45:48 已有 11 个任务成功并持久化，包含 heartbeat、vote_kick_check、提醒、关键词清理与上下文清理；本轮 miss 均为 0。
- 07:20 自然 `ttl_cleanup` 成功并于 07:23:35 写入 `scheduler_metrics`；`task_execution_history` 只覆盖进入事务审计的任务，07:51 共有 209 条历史记录但最近 1 小时为 0，不冒充所有调度任务都成功。
- MoryFansBot 的 Bot/Web 进程仍在线且 9919 返回 302；MediaOps-COO 三个 PM2 进程 online，8318 `/livez` 与 8819 `/api/health` 均 200。
- 非保留精确进程 0；Docker/containerd/Nginx/sing-box inactive，TokenLab/TokenPass inactive+masked，failed units 为 0。

## 证据边界

- `dynamic_jobs:not_observed` 与 `direct_registry:false` 明确表示巡检没有跨进程直接读取 APScheduler 注册表；有限结论只覆盖认证重建回执、后续失败扫描、持久指标和配置的业务回执。
- 本轮未制造真实 Telegram 用户消息、投票或踢人；真实外部事件继续等待自然流量。
- 12:00 `burn_orphan` 首次自然执行尚未到时，Goal 保持活动并在该时间点后继续核验。

## 部署三选一

**已部署**：v5.42.19 已在既定 `/home/ubuntu/mory_assistant` 上线，并完成版本、哈希、双服务、日志、数据库、调度、业务回执和保留项目边界验收。
