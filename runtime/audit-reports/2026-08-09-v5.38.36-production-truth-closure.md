# v5.38.36 生产真相审计与闭环报告

日期：2026-08-09（Asia/Shanghai）

## 结论

本轮没有发现可确认的 P0 全局停服或数据丢失。已修复并部署 7 类可复现暗病：配置热重载不重编排、关键监控漏栏目、监控使用错误真相源、报表伪官方 API、资料审核重复 getChat、看门狗不可诊断且无轮转、部署器断线半部署仍返回 0。

## 生产基线证据

- 审计时 v5.38.35 双服务 active/enabled、NRestarts=0、health 200；当前 PID 窗口无 ERROR/CRITICAL/Traceback/模型失败。
- 24 小时历史存在重启窗口的 closed DB、模型超时降级、NameError 与 process_restarted_before_completion；均未在审计时当前 PID 复现。
- 当日真实业务回执：mystic_morning 09:05、mystic_afternoon 13:05、mystic_evening 20:35、daily_report 09:10 均 success；三档 task_log receipt id 为 15511/15515/15522。
- `scheduler_metrics.last_error` 为无失败时间的累计历史字段；最近日志无 cannot commit/sub-exceptions，不能把累计错误误报为当前故障。

## 修复清单

1. `TaskScheduler.refresh_tasks()` 按最新 config replace/add/remove 自管 job；配置刷新失败恢复旧配置和旧任务集。
2. 30 分钟关键任务检查纳入 mystic 三档并读取实际配置时间；Loop L4/L5 改以 `task_execution_history` 四态和 `scheduler_metrics` 为主，查询失败 fail-close。
3. 日/周/月报删除恒为 None 的 Telegram 官方统计分支，明确数据源为 Bot 事件自统计 + Telegram 实时人数。
4. 已获取的 Telegram `chat_info` 透传资料广告检测，避免同一路径重复 getChat。
5. watchdog 严格解析 health JSON、保留 stderr、root 直调 systemctl、日志 5 MiB×3 有界轮转，并纳入 Git。
6. 清除 4 个陈旧 skip，按当前去萌化合同重写断言；全仓最终无 skip。
7. 部署器改为 40 文件分批、断线重连并重传当前批次；最终验证失败返回退出码 1。

## 本地门禁

- `.venv\\Scripts\\python.exe -m pytest tests/unit/ -q -p no:cacheprovider -rs`：963 passed，0 skipped。
- `compileall core modules tasks dashboard scripts -q`：通过。
- 项目 flake8 必查集、改动报表/watchdog 文件：通过。
- 项目 mypy 必查 4 文件与改动 scheduler/report/watchdog 文件：通过。
- `verify_db_methods.py`：199 个委托方法，无缺失、无孤儿。
- `check_config_sync.py`、`doc_consistency.py`、`check_deploy_ready.py`：全部通过。

## 部署与恢复事实

- 首次短时本地执行上限使部署进程脱离输出管道；核实远端无部署子进程后终止精确本地 PID。
- 第二次部署在 80/408 文件处 SSH 断开，脚本错误返回 0；旧服务仍运行但磁盘代码部分更新。
- 采用精确 18 文件逐项临时上传、原子替换和 SHA-256 校验，安全合并 config（远端 271 键，敏感值保留），远端 compileall 后单次重启双服务。

## 生产验收

- mory-assistant PID 2174607、mory-dashboard PID 2174608；均 active/running，NRestarts=0，启动时间 23:01:25 CST。
- `/api/health` HTTP 200，body `{"status":"ok"}`；`version.py=v5.38.36`，config `_CONFIG_VERSION=5.38.36`，config 271 键。
- 当前启动注册 40 个任务；问候全部关闭，用户主动栏目仅 mystic_morning/afternoon/evening。
- 触发内容不变的 reload_flag 后日志明确出现“调度任务已按热重载配置刷新”和“Dashboard配置已同步到Bot内存与调度器”，任务仍为 40 个。
- 新 PID 连续 23:02、23:03、23:04 heartbeat 均 executed successfully；启动扫描四态回执 success。
- 真实 VPS 上运行新版 L4/L5：均 OK；近 1 小时 success=1，failed/running/aborted=0，stale running=0，当前 scheduler metrics error 空。
- root 身份运行 watchdog：退出 0，health ok，`watchdog_fail_count=0`。非 root 手工运行因 root 日志权限失败，符合 root cron 的真实运行身份边界。

## 剩余边界

- 新版本发布后的下一次用户面 mystic 定时回执要到次日 09:05；本轮未为验收额外发送重复 Telegram 广播。发送链本日发布前已有三档真实 success 回执，发布未修改其内容/发送实现。
- Dashboard 停止时仍会出现 gevent `greenlet is being finalized` 噪声；当前新进程窗口无错误，不影响服务，但后续可单独治理停机日志质量。
- systemd `WatchdogSec=0`，当前可靠性依赖 root cron 每 2 分钟运行外部 watchdog；本轮已验证该真实路径。
