# v5.38.35 个人资料关联频道广告修复验收

## 事故证据

- 生产用户 `8562992698` 于群 `-1003004701688` 两次发送“凎活啦”，消息 `65234`、`65236` 初始均为 `is_ad=0/deleted=0`。
- `getChat(8562992698)` 的 Bio 为空，但 `personal_chat=-1004432682202`；频道标题“财天下飞机进群结演员结算频道”，简介为“别人准备干……别人赚钱了……”式扩写。
- 根因：旧资料链只读 Bio，未采集 Telegram `personal_chat`。

## 修复

- 资料检测读取关联频道标题、username 与完整 description。
- 去除空格/标点后，以平台暗语、拉群动作、商业招揽、频道载体中的三个独立锚点判定，覆盖拆字、同义改写与反思式扩写。
- 单独词不定罪；航班交流群、演员工作结算通知频道、单纯励志反思均为固定正常反例。
- 资料强证据在短句路径先于 AI 回复执行，统一走 `enforce_ad_user()`。

## 验证

- 定向广告链：58 passed。
- 全仓：949 passed，4 skipped；compileall、flake8、mypy 通过。
- `verify_db_methods.py`：199 个委托方法通过；配置同步、文档一致性通过。
- 生产真实探针：`source=personal_chat`，四锚点全部命中，`is_ad=true/score=3`。
- 生产处置：Telegram 状态 `restricted`；`blacklist=1`、`global_blacklist=1`、`mute_records=1`。
- 两条消息均 `is_ad=1/deleted=1`，再次调用 Telegram 删除接口均返回 `already_absent`。
- 运行态：双服务 active、NRestarts=0、health 200、VPS `v5.38.35`；3 个运行文件 SHA-256 本地/远端一致。

## 部署与回滚

- 部署：增量上传 `modules/ad_profile_signals.py`、`core/handlers/security_handlers.py`、`version.py`。
- 生产备份：`/home/ubuntu/mory_assistant/backups/deploy_v53835_incremental_20260809_220251`。
- 回滚：从上述目录恢复三个文件，远端 `py_compile` 后重启双服务并复验 health/version。
