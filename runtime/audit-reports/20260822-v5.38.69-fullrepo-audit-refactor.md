# 全仓深度优化完工报告（v5.38.69）

> 分支 `refactor/audit-20260822`（基于 main e8163a3）· 2026-08-22 · 施工：贾维斯（OpenCode）
> 老板确认方式：第二阶段 6 张确认卡片获"全部OK"后一次性施工。

## 一、变更总览（5 个提交）

| 提交 | 内容 |
|------|------|
| 37dd830 | 清理：4 个零使用依赖（structlog/psutil/diskcache/pydantic-settings 及 pydantic 链）从 requirements.in/txt/lock 摘除；删 9 张旧版欢迎图 + 4 张后缀错位背景图；.better-harness/.codebuddy 与 2 个 runtime 过程产物移出跟踪；data/*.json 三个生产必需文案文件入库；assets 忽略规则精确化 |
| 8a41f83 | 高危修复：shop 兑换单事务+失败回滚；lucky_wheel 扣费开奖同锁+失败自动退费+发消息挪锁外；user_repo 仅按确凿会话失效错误删档（新增 core.helpers.is_permanently_gone_chat_error，4 处调用点收敛）且 delete_user 补回滚；report/warning/relay 通知 HTML 转义；federation/invite/security_center 绕锁写库改走全局 _db_lock |
| 351f891 | legacy 壳拆除：modules/auto_tasks.py（3414 行）删除；start_background 引擎迁入 tasks/task_scheduler.py；report_fault 统一至 tasks.support.fault_reporter（12 引用点）；健康检查关键任务簇迁至 tasks/support/critical_tasks.py；checkin/lottery/scheduler_api 改用 get_scheduler_instance()；3 个测试文件随迁，2 个仅测死代码的用例移除 |
| 84fec90 | 孤儿模块下线：core/handlers×5 + modules×36（import 层面全仓三重验证零引用）；数据库表原地保留待数据治理专项；过时的 v5.35.0 模块存在性测试段移除 |
| f4db889 + cc8f655 + docs | 结构治理：html_page.py 4722 行前端拆为 templates/index.html + login.html（deploy_vps SCAN_DIR_EXTS 增加 dashboard .html 映射防线上白屏）；database._init_tables 1871 行拆为 15 个域分片+索引片（语句逐字、顺序不变）；周报/月报三连克隆工具函数抽至 tasks/support/report_utils.py；安全加固 ProxyFix 改条件信任（DASHBOARD_TRUST_PROXY，默认关）与 sudo 密码 stdin 隐蔽通道；pytest.ini 死 marker 删除；ai_engine 门禁注释对齐实际；ad_detector 日志参数化 |

## 二、验证证据（全部本地实测）

- `python -m pytest tests/unit/ -q` → **1199 passed**（基线 1237 − 36 条被删模块存在性断言 − 2 条仅覆盖已删 legacy 的用例）
- `scripts/doc_consistency.py` exit 0（METRICS 六项一致 + 文档卫生五断言过）
- `scripts/check_config_sync.py` exit 0
- `scripts/verify_db_methods.py` → "✅ DB 方法注册验证通过：217 个委托方法"
- `scripts/check_deploy_ready.py` → "✅ 部署就绪"
- records-autopilot `--apply --strict` pass，同 fingerprint `--verify-only --strict` pass（receipt d0541573fb1eb0d0b05b0440.json）
- Dashboard 冒烟：test client GET `/` = 200 且渲染 LOGIN_PAGE（3248B，来自新模板文件）；`/api/health` 如实返回 degraded（无 Bot 运行态的预期表现）
- database 拆分后 CREATE TABLE 计数 175 不变；DB 方法门禁无缺失无孤儿

## 三、遗留风险与边界

1. **VPS 未部署**：本批改动仅在本地分支生效；VPS 仍运行 v5.38.68。部署时 html_page 拆分依赖 deploy_vps 新增的 dashboard `.html` 上传映射，务必走本仓库 deploy_vps.py 完整流程。
2. **孤儿模块同名表**：bot_list/word_cloud/valid_speak/punishment_center/entertainment_games/afool_member 等表结构保留，含历史数据；表级清理需另开数据治理专项（含备份）。
3. **ProxyFix 行为变化**：若生产 6616 确有反代，需在 .env 设 `DASHBOARD_TRUST_PROXY=true` 再部署，否则限流按直连 IP 收紧（更严不更松，但日志 IP 会是代理 IP）。
4. **cleanup_vps_full/ssh_helper** 的 sudo stdin 改造仅本地语法与逻辑审查，真实 VPS 连接验证待下次运维窗口。
5. **data/*.json 入库**内容经关键词扫描无敏感信息，但建议老板知悉这三个文件现在进 git。

## 四、指标对比

| 指标 | 前 | 后 |
|------|----|----|
| modules 业务 .py | 139 | 102 |
| core 业务 .py | 82 | 77 |
| auto_tasks.py | 3414 行（33 个死 _job_ 函数） | 已拆除 |
| 最大单文件 | html_page.py 4967 行 | dashboard/app.py ~200 行级（前端独立模板） |
| 最大单函数 | _init_tables 1871 行 | 15 个域分片 |
| 单元测试 | 1237 passed | 1199 passed（净减 38 条死断言） |
