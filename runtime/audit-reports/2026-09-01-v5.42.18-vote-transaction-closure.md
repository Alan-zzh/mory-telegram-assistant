# v5.42.18 投票事务与调度真相生产闭环

## 结论

- 2026-09-01 06:43 CST 部署 `325d0ebf7b10`；远端版本 `v5.42.18`。
- 投票链改为完整事务回滚、截止后拒绝收票、`processing` 可恢复状态和 Telegram 侧 `kicked` 幂等确认；消息发送结果未知时保留记录并上抛。
- 调度巡检累计失败行新增 `error_scope`，当前成功与历史错误不再混写成当前故障。

## 本地门禁

- 目标回归 38 passed；全仓单元测试 1451 passed。
- `verify_db_methods.py`：219 个委托方法无缺失、无孤儿。
- `doc_consistency.py`、编译、diff、部署就绪与 records-autopilot 双回执均通过。
- 独立专家三轮反向审查，最终无 P1/P2 上线阻断。

## 部署与回滚

- 部署器上传 6 个变更文件；代码 tar 与数据库在线快照均已校验。
- 代码备份：`backups/code_deploy_1788216178.tar.gz`，SHA-256 `42bb122b46cd23b764b518948f8c6e203fbfe7215c6d3b9aa5b9d5c08fce41d4`。
- 数据库备份：`backups/mory_pre_migration_1788216211_f1374a33d2cef03d.db`，完整性 `ok`。
- 回滚：`cd /home/ubuntu/mory_assistant && tar -xzf backups/code_deploy_1788216178.tar.gz && sudo systemctl restart mory-assistant mory-dashboard`。

## 生产验收

- `modules/vote_kick.py`、`scripts/puzan_loop_monitor.py`、`version.py` 远端 SHA-256 与本地一致。
- Bot 与 Dashboard 均 `active/running/enabled`、`NRestarts=0`；health 200；新进程启动窗口错误数为 0。
- 数据库 `integrity_check=ok`；生产只读巡检 17 pass、0 evidence gap、0 failed。
- 部署后 `vote_kick_check` 于 06:45 成功，`success_count=8671`、`fail_count=8`、`miss_count=0`；累计旧错明确为 `historical`。
- 生产代码隔离探针验证：过期回调拒绝收票、`processing` 中断恢复、未知发送保留记录，三项通过；未调用真实 Telegram。
- MoryFansBot 进程与 9919 监听正常，MediaOps-COO 三个 PM2 进程在线且 `/livez` 200；非保留进程为 0，TokenLab/TokenPass 单元保持 masked，Docker/containerd 保持 inactive。

## 边界

- `live_event_boundary`：本轮没有制造真实投票、真实踢人或真实 Telegram 消息；用户可见行为以生产隔离 evaluator、真实调度成功与后续自然事件为证据边界。
- 部署时旧 Dashboard worker 退出曾打印 gevent finalization 噪声；新 PID 启动窗口为 0 错误，未形成当前故障。
