# v5.38.65 入群资料邀请引流漏判生产闭环

日期：2026-08-19

## 结论

截图账号 UID `8858734300` 的漏判由两层缺口共同造成：入群和验证码放行时 Telegram 均暂未返回 Bio；数小时后发言时虽已读到 Bio，但“群邀请链接 + 多一条路试试”和正文“看我”没有形成高置信组合。v5.38.65 已补充有界资料复审与精确组合证据，并完成生产部署和真实账号处置。

## 事故证据

- 2026-08-18 18:13:40：验证码放行复审 `bio_available=False`，资料与头像均 `score=0 outcome=pass`。
- 2026-08-18 23:54:10：消息 `68183` 为“都TMD看我，搞不了几k你直接骂死我”。
- 同请求已读到 Bio “`t.me/+...` + 多一条路试试”，旧生产仍未命中。
- 修复前数据库：消息 `is_ad=0/deleted=0`；本地/全局黑名单和禁言记录均为空。

## 修复与反例边界

- Bio 暂不可见时，对同一 UID 在 30 秒、5 分钟、30 分钟做三次有界复审；离群、群管、白名单或调度器不可用时显式跳过/降级。
- 群邀请链接只有再叠加“多一条路试试/看看”等规避引流话术才是高置信资料广告。
- 正文明确“看我”且资料已命中上述邀请引流组合时，当前消息记录为直接桥接广告。
- 裸个人链接、普通绑定频道、普通备用摄影群邀请继续放行；头像仍不能单信号处置。

## 本地门禁

- 定向回归：`341 passed`。
- 全仓单测（真实项目环境）：`1184 passed`。
- 干净提交门禁：版本五源、文档一致性、配置同步、Git 来源均通过。
- DB 方法注册：`208` 个委托方法通过。
- 提交：`434f222`。

## 部署与回滚

- 生产版本：`v5.38.65`。
- 备份：`/home/ubuntu/mory_assistant/backups/ad_invite_v53865_20260819_002107.tar.gz`。
- 远端四个运行文件 SHA256 与提交一致。
- 服务：Bot PID `3168404`、Dashboard PID `3168405`，均 `active/running`、`NRestarts=0`；health HTTP 200。

## 用户可见验收

- 生产解释器：截图 Bio 判定 `is_ad=true, score=3, source=bio_invite_teaser`；正文桥接判定为 true。
- 正常“备用摄影交流群 + t.me/+...”反例 `is_ad=false, score=0`。
- 统一处置回执：`muted=true, blacklisted=true, deleted_count=1, evidence_persisted=true`。
- Telegram 读回：账号 `restricted` 且 `can_send_messages=false`；再次删除消息返回 `message to delete not found`。
- 数据库读回：消息 `68183 is_ad=1/deleted=1`；本地黑名单、全局黑名单、永久禁言记录均存在；`PRAGMA integrity_check=ok`。

## 边界

三次延迟复审的真实时间窗需等待后续新成员事件观察调度日志；本次已在生产代码解释器和单元级调度器完成正反例验证，当前涉事账号与消息已取得真实 Telegram/数据库回执。
