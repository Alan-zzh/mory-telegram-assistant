# v5.42.16 生产闭环

## 结论

- 提交 `e0eff9342acff3b05bd86e7fcd859a915beb6332` 已部署到既定生产目录。
- Mory Bot 与 Dashboard 均为 `active/running/enabled`，`NRestarts=0`，health HTTP 200。
- 受控重启验证了新停机顺序：调度器关闭 → 启动维护线程完成并退出 → 数据库连接关闭 → 新进程启动。
- 部署后、受控重启后各执行一次 production-truth，均为 17 pass / 0 evidence_gap / 0 failed。
- 服务器项目进程只保留 Mory、MoryFansBot、MediaOps-COO；非保留 TokenLab/TokenPass 六个单元均为 inactive+masked，Docker 容器为空。

## 本地门禁

- 目标回归：66 passed。
- 全量 unit：1432 passed in 36.74s。
- `py_compile`、项目正式 Ruff 8 文件清单、mypy 4 文件清单通过。
- DB 方法 219 个无缺失/孤儿；配置同步、文档一致性、`git diff --check` 通过。
- records autopilot strict apply/verify 同 fingerprint 通过；独立复审无 P0/P1/P2。

## 生产证据

- 版本：远端 `version.py` 读回 `v5.42.16`。
- 哈希：`main.py`、`tasks/task_scheduler.py`、`scripts/puzan_loop_monitor.py`、`scripts/project_audit_control.py`、`version.py` 的远端 SHA-256 与本地逐项一致。
- 资源：CPU 约 2.3%，可用内存约 80.7%，根盘 56%，最近 1 小时无 OOM 证据。
- 业务/调度：当前 1 小时 failed/running/aborted 均为 0；转化窗口为真实秒级查询，当前结果为 0；最近三次 mystic 持久业务回执有效。
- 数据库：在线迁移前快照 SHA-256 为 `5f828889eccc004b263eaf6920fa705b26c90f44602a39857ea5cf2c878f19bc`，迁移后完整性通过。
- 监控：L1-L6 均正常；watchdog 新鲜且无旧 cron 残留。

## 非保留项目防复活

- 新增屏蔽：`tokenlab-bridge.service`、`tokenlab-login.service`、`tokenpass.service`、`tokenpass-gpt-openai-guard.service`、`tokenpass-gpt-openai-guard.timer`。
- 既有屏蔽复核：`tokenlab-proxy-health.timer`。
- 原 unit 备份：`/var/backups/mory-nonretained-units-20260901T0240/`，目录 `root:root 0700`，文件 `root:root 0644`，五份文件均已读回 SHA-256。
- 非保留进程、cron 引用、Docker 容器均未发现；保留项目的 systemd/PM2 持久态正常。

## 回滚

- 代码：`cd /home/ubuntu/mory_assistant && tar -xzf backups/code_deploy_1788201176.tar.gz && sudo systemctl restart mory-assistant mory-dashboard`。
- 数据库：停服务后以 `backups/mory_pre_migration_1788201212_e699cbdda5e0ee0b.db` 恢复 `mory.db`，再按 runbook 校验完整性后启动。
- 非保留 unit：删除上述五个 `/etc/systemd/system/<unit>` 的 `/dev/null` 链接，从 `/var/backups/mory-nonretained-units-20260901T0240/` 逐个复制回原路径，然后 `systemctl daemon-reload`；仅在老板明确要求恢复这些项目时执行。

## 边界

- health 仅作为 liveness；完成结论同时依赖版本/哈希、服务状态、启动窗口日志、数据库、调度和业务回执。
- `conversion_1h=0` 是当前真实窗口结果，不表示历史没有转化。
- Mory 自动巡检与 MoryFansBot 权限/看门狗定时器属于保留项目运维，不是额外项目，继续保留。
