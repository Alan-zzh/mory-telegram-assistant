# v5.38.68 签到误封与自助解封生产验收

日期：2026-08-22（Asia/Shanghai）

## 完成结论

- 生产已切换至 v5.38.68，`AD_SELF_UNBAN_ENABLED=true`，配置权限保持 `0600`。
- “私信开了”“签到”等 8 条普通反例均为非广告；5 条收益、招募、灰产与明确联系方式组合均保持高风险广告。
- 处置事件迁移已到 `0008_ad_enforcement_events`；事件表 23 列、3 个索引，SQLite `integrity_check=ok`。
- Hank（UID 仅用于本次事故恢复）已走统一恢复链：本地黑名单、全局黑名单、禁言记录、可疑追踪均读回 0；Telegram 主群成员读回 `restricted` 且 `can_send_messages=true`。
- Telegram Bot API 已实际接受双按钮卡：自助复检回调前缀为 `ad_self_review`，管理员入口为 `https://t.me/Moryfansbot`；验收卡发送至管理员私聊后删除，未在主群制造测试封禁。

## 生产证据

- 运行态：`mory-assistant`、`mory-dashboard` 均为 `active`，`NRestarts=0`；Dashboard `/api/health` 返回 HTTP 200、`status=ok`。
- 版本与文件：`version.py` 及 14 个受影响运行文件逐项 SHA-256 与提交 `c208ff9` 一致；回放脚本入口修复提交为 `f7b2b41`，生产脚本 SHA-256 与该提交一致。
- 数据库：升级前创建代码、数据库和配置备份；Alembic 当前版本为 `0008_ad_enforcement_events`。
- 规则探针：普通反例 8/8 不封，真实广告 5/5 高风险封禁。
- 真实签到链：上线后普通群成员发送“签到”，`message_snapshots` 读回 `is_ad=0, deleted=0`，四项广告状态均为 0，并完成 `checkin` 获得 5 积分。
- 历史回放：141 条旧广告快照中 119 条仍为广告，22 条仅列入人工候选，`auto_restore_count=0`；Hank 位于候选报告中，其余账号未自动恢复。
- 本地回归：相关闭环测试 35 项通过；全仓 unit 1237 项通过。

## 异常与处理

- 首次健康探针误用了旧端口 5000，返回连接失败；读取 systemd 与监听状态后改用真实 Dashboard 端口 6616，得到 200/ok。
- 常规部署器漏传新增 Alembic 文件；逐文件哈希发现后精确补传并执行升级，未覆盖生产配置或数据库。
- 回放脚本首次从 `scripts/` 直接启动时无法导入项目模块；已修复入口并补充“项目目录外启动”回归，重新部署后回放成功。
- 本地部署器进程未返回最终退出码；生产结论由远端版本/文件哈希、双服务、数据库迁移、配置、日志和业务探针独立闭环，不以该进程状态代替。

## 剩余证据缺口

- 解封后日志窗口内尚未出现 Hank 新的真实“签到”；因此“本人下一次真实签到成功”仍待实时事件补证。生产已有另一名普通成员的真实签到成功回执，且 Hank 的账号持久态和 Telegram 发言权限均已恢复。
- 没有可用的隔离 Telegram 测试账号，未对真实用户制造封禁来演示错误用户点击、高风险拒绝和自动恢复；这些分支由生产同哈希代码的单元/链路测试覆盖，双按钮本身已有 Bot API 发送回执。

## 部署选择

已部署。生产版本 v5.38.68；双服务、health、文件哈希、迁移、配置、规则探针、Telegram 按钮和 Hank 恢复均已取证。
