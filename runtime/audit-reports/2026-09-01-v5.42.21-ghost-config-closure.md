# v5.42.21 幽灵配置清理生产闭环

## 结论

- 2026-09-01 08:26 CST 部署提交 `fb0fdfe335a4`；远端版本 `v5.42.21`。
- 11 个确认退役的顶层配置统一由 `REMOVED_CONFIG_FIELDS` 管理；运行时保存、部署安全合并、Dashboard 和生产漂移巡检均阻止其复活。
- 生产 `config.json` 已剥离全部 11 个退役字段，旧复数凭据容器 `API_KEYS` 不再落盘；当前运行凭据仍只从 `.env` 注入。

## 本地门禁

- 聚焦回归 69 passed；全仓单元测试 1461 passed。
- `compileall`、Ruff、mypy、DB 方法注册 219 项、配置同步三项断言、文档一致性与 diff check 全部通过。
- records-autopilot 首次发现病历条目超预算 1 条且自动动作失败，受控重跑治理引擎后裁剪至 50 条；随后 apply/verify strict 同指纹通过，旧正文仍由 Git 保存。

## 部署与回滚

- 部署器哈希预检 345 个文件，只上传 9 个差异文件；336 个相同文件跳过。
- 代码备份：`backups/code_deploy_1788222347.tar.gz`，权限 0600，SHA-256 `aa25396c65a34ee6eb7e594c375f457e85bdd36031f60f219ad1da8549a18ef7`。
- 配置备份：`backups/config_20260901_082617_11fe1c4236b54381a6addd521a9eb419.json`，权限 0600。
- 数据库备份：`backups/mory_pre_migration_1788222382_ac763f420ea385f5.db`，SHA-256 `44f18df17c879d31c9560449f19dc8d5f445479ce65b8973a58a90ecd4250a63`。
- 回滚：恢复上述代码与配置备份后重启 `mory-assistant`、`mory-dashboard`；本次未改 schema，数据库快照仅作发布保险。

## 生产验收

- `version.py`、`core/config_compat.py`、`core/deploy_utils.py`、Dashboard 配置 API 和生产巡检文件哈希均与本地一致。
- Bot 与 Dashboard 均 active/running/enabled、`NRestarts=0`，health 200；启动窗口日志干净，数据库完整。
- 只读项目巡检：生产 17 pass、漂移 7 pass、0 evidence gap、0 failed；`confirmed_removed_keys_present=[]`。
- 生产配置直接读回：`REMOVED_PRESENT=[]`、退役集合总数 11；备份与现行配置权限均为 0600。
- MoryFansBot 的 Bot/Web PM2 进程 online、9919 返回 302；MediaOps-COO 三个 PM2 进程 online，8318 `/livez` 与 8819 `/api/health` 均为 200，Gemini Web2API 端口 48319 在监听。
- 非保留 TokenLab/TokenPass 四个精确单元均 inactive+masked；Docker/containerd inactive+disabled；服务器项目进程扫描只发现三个保留项目及其必要 PM2 日志模块，failed units 为 0。
- 内存可用 6136 MiB（约 79.8%），根盘使用 56%，最近一小时无 OOM；本轮没有为了“省资源”停止任何保留项目组件。

## 证据边界

- 生产业务回执覆盖已配置的传统文化定时任务，不包含本轮制造的 Telegram 用户消息；未用假消息冒充真实流量。
- 12:00 `burn_orphan` 部署后首次自然执行尚未到时；Goal 保持活动并在自然执行后复核。

## 部署三选一

**已部署**：v5.42.21 已在既定 `/home/ubuntu/mory_assistant` 上线，并完成版本、哈希、配置备份、双服务、数据库、巡检、保留项目和非保留自启边界验收。
