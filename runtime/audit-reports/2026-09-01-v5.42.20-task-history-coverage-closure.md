# v5.42.20 事务任务历史覆盖口径生产闭环

## 结论

- 2026-09-01 08:02 CST 从 `main` 提交 `57949efa4be6` 部署到既定 `/home/ubuntu/mory_assistant`，远端版本读回 `v5.42.20`。
- 生产 `task_execution_history` 并非空表：共 209 行，最新为 `mystic_evening success`；最近 1 小时 0 行只表示该窗口没有进入 `TaskTransactionManager` 的事务任务。
- L4/L5 人类输出和结构化审计均使用 `transactional_*` 字段并附 `TaskTransactionManager_only` coverage；旧字段只作为兼容别名保留。
- 不修改 `TaskScheduler._execute_task` 写入语义；全部 APScheduler job 的聚合运行事实继续由 `scheduler_metrics` 承担，避免双记和假成功。

## 本地验证

- 聚焦回归：76 passed；全仓单元测试：1455 passed。
- `compileall`、Ruff、mypy、`doc_consistency.py`、`check_deploy_ready.py` 全部通过。
- records-autopilot 首次发现 `AI_DEBUG_HISTORY.md` 超预算 1 条并失败关闭；将同类教训合并到既有 6.51 后，apply/verify strict 同指纹通过。
- 独立只读复核发现 L5 残留含糊字段和 L4 未展示最新记录；修正后无 P0/P1，相关回归重新通过。

## 部署与回滚

- 哈希预检 345 个文件，只上传 7 个变化文件，338 个相同文件跳过。
- 代码备份：`backups/code_deploy_1788220934.tar.gz`，SHA-256 `b8295b6436a7d2fa46db2f72fe73ab7863fb4c406475d8c01ae8c66881af41cd`。
- 数据库备份：`backups/mory_pre_migration_1788220966_a50b9bce51221935.db`，SHA-256 `140682273a632d57e3261ab1b7d3346fc4aa613e5ede749cf2d05658f8fabb85`。
- 回滚方式：恢复代码 tar 后重启双服务；只有迁移相关问题才在停写后恢复已校验数据库快照。

## 生产验收

- Mory Bot PID 3464082、Dashboard PID 3464083，均 active/running/enabled、NRestarts=0；health 200，启动窗口 ERROR/CRITICAL 计数 0。
- 受影响文件 `puzan_loop_monitor.py`、`project_audit_control.py`、`deploy_utils.py`、`version.py` 的远端 SHA-256 与本地逐项一致。
- 生产只读审计 17 pass / 0 evidence gap / 0 failed；数据库 integrity=ok、外键违规 0。
- 调度事实回执：`coverage=transactional_tasks_only_1h+current_metrics_1h+cumulative_metrics(4)`、`history_total=209`、`history_latest_start=1788179700`。
- 调度注册回执：认证重建 `managed_jobs=41`；直接跨进程注册表仍明确为 `not_observed`。
- MoryFansBot 的 Bot/Web 进程在线且 9919 返回 302；MediaOps-COO 三个 PM2 进程 online，8318 `/livez` 与 8819 `/api/health` 均 200。
- Docker、containerd、Nginx、sing-box inactive；TokenPass 与 TokenLab Login inactive+masked；systemd failed units 为 0。

## 证据边界

- 本次修复的是监控语义误判，不制造 Telegram 写动作；自然消息、投票或踢人仍须等待真实事件，不能用隔离探针冒充。
- `scheduler_metrics` 的 EXECUTED 只证明 callable 未向外抛异常，不等于业务动作成功；事务业务结果仍以 `task_execution_history` 四态为准。
- 12:00 `burn_orphan` 首次自然执行尚未到时，Goal 保持活动并在到点后继续核验。

## 部署三选一

**已部署**：v5.42.20 已在既定生产环境上线，并完成版本、哈希、双服务、日志、数据库、调度覆盖口径与三项目边界验收。
