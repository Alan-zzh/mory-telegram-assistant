# v5.42.17 入群资料复审告警真相与修复回执

## 生产事实

- 2026-09-01 03:23 CST 只读检查最近 24 小时：`retry_exhausted` 25 次，涉及 25 个唯一 UID/群组合，无重复组合。
- 25 次终态前一轮均为 `fetch_failed=False`；没有 `membership_unknown` 或 `scheduler_unavailable`。
- 这些记录代表 Telegram 成功返回资料但用户未设置 Bio，不是服务器、网络、调度或数据库故障，也未触发禁言、拉黑或持久重试状态。

## 修复

- 保留 30 秒、5 分钟、30 分钟三次有界复审和延迟后广告命中能力。
- 成功空 Bio 终态改为 `INFO outcome=complete reason=bio_absent_after_retries`。
- `get_chat` / `get_chat_member` 抛错或返回空对象时保留明确 `WARNING` 降级原因。

## 本地验证

- `tests/unit/test_member_admission_gate.py`: 19 passed。
- 广告资料与处置相关六个测试文件：459 passed。
- 全仓单元测试：1438 passed；Ruff、mypy、DB 方法、配置同步、文档一致性与编译检查通过。

## 生产闭环

- 提交 `b784998ec531` 经既定部署器发布；345 个文件中上传 5 个、跳过 340 个，数据库在线快照与迁移门禁通过。
- 远端版本 `v5.42.17`；`version.py` SHA-256 `e5b577b4d280ee0685754055070c50323e769518cb4336f4611fb56340205f9b`，`core/handlers/member_handlers.py` SHA-256 `09808664afc62e12548c29009f957a4b29d1f54beb991f27500aa00465d67365`，均与本地一致。
- 双服务 `active/running`、`NRestarts=0`，新进程启动窗口无 WARNING；health 200，数据库、权限、调度和只读业务回执在 production-truth 17/17 通过。
- 生产代码隔离 evaluator：正常空 Bio 仅 INFO complete；空 Chat 响应为 `bio_fetch_failed_retry_exhausted`；空成员状态为 `membership_query_failed_retry_exhausted`，三例通过。
- `live_event_boundary`：本轮没有等待或制造真实新成员入群；自然入站后的首条同类终态留给持续 Goal 观察，不冒充真实 Telegram 事件。
