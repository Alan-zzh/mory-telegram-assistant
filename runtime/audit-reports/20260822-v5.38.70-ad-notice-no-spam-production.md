# v5.38.70 广告处置播报降噪生产回执

## 结论

- `high` 确证广告继续永久禁言、删消息与双黑名单，但不再发送群内说明卡。
- 仅 `low` / `ambiguous` 疑似误判事件可显示自助复检卡；同一群 24 小时最多一张共享卡，按点击者本人和卡片所在群定位其根事件。

## 验证

- 目标回归：38 passed；全仓 unit：1202 passed。
- DB 方法注册、配置同步、文档一致性、部署就绪门禁均通过。
- 生产版本：`v5.38.70`；Bot PID `1952375`、Dashboard PID `1952376`，双服务 `active`、`NRestarts=0`、health HTTP 200。
- `modules/ad_enforcement.py` 等 5 个受影响文件 SHA-256 与本地 `main` 一致。
- 生产清理：选中 6 张未过期 `high` 说明卡，5 张 Telegram 删除成功、1 张已不存在、0 失败；`remaining_high_notice_slots=0`。
- 运行态隔离探针：`high` 事件调用说明卡发送入口返回 `0`，测试 Bot 的发送方法未被调用。

## 边界

- 本轮没有在真实群中制造新的广告或疑似误判事件，避免再次打扰群聊；实际群面证据为已有 6 张实锤卡被清理且没有补发。
- 生产数据库清理前已生成权限 `0600` 的一致性备份 `backups/mory_pre_v53870_notice_cleanup.db`。
