---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-20T08:40:25.635Z
provider: 
---

# 通用项目业务说明书 (Project Map)

## 1. 🎯 业务核心目标
- **项目名称**：[在这里填入当前项目的名字，如：自动化发帖脚本]
- **核心用途**：[在这里填入这东西是干嘛的，如：实现多账号全自动定时发布]
- **业务红线**：[在这里填入底线，如：绝对不能因为报错导致程序卡死崩溃]

## 2. 🗺️ 历史文档优先原则 (强制规范)
- 根目录必须包含并维护 `project_snapshot.md`、`CHANGELOG.md` 和 `AI_DEBUG_HISTORY.md`。
- **复用优先**：遇到任何 Bug 或需求，第一步是查阅这三个文档。文档里有现成方案的，直接复用；文档里踩过坑的，绝对避开；只有全新的问题，才允许写新逻辑。

## 3. ⚙️ 技术边界
- 自动识别技术栈，按当前框架最佳实践编写代码。所有代码注释必须为中文。
- 遇到报错自动写入本地 `logs` 文件夹，严禁全屏打印一堆红字代码给 User 审查。