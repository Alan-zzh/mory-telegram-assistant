---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-17T16:11:26.348Z
provider: 
---

### 📋 项目规则内容（一键粘贴版）

**创建步骤：**
1. 在 CodeBuddy 左侧切换到 **“项目规则”** 标签页（就是你截图里那个界面）。
2. 点击 **“+ 创建规则”**。
3. 规则名称填：`project_base_guide`
4. 规则类型选：**`总是`**
5. 把下面内容完整粘贴进去。

```markdown
# 🧰 通用项目开发底座规则 (跨技术栈·小白友好·AI记忆增强版)

> **设计目标**：无论本项目是 Python 脚本、Telegram Bot、Web 网页、小程序、桌面软件还是其他任何形态，AI 都能根据此规则快速理解上下文，并自动维护项目的“记忆系统”。

## 一、 🏷️ 项目身份动态识别
- **项目代号**：`{{PROJECT_NAME}}` *(请口头告知 AI 本项目的具体名称，例如“小汪记账”、“自动签到助手”)*
- **技术栈类型**：*由 AI 根据项目文件自动推断（Python / Node.js / Web / Go / Rust / 其他）*
- **当前开发阶段**：*初始化 / 功能开发 / 调试修复 / 部署维护*

## 二、 🗺️ 通用目录约定 (AI 行动指南)
为了让 AI 在陌生的项目结构里也能快速找到门路，请尽量遵循以下命名习惯。AI 在读取文件时应优先按此映射理解意图。

| 目录/文件关键词 | 预期用途 | AI 处理策略 |
| :--- | :--- | :--- |
| `src/`, `core/`, `lib/` | 核心源码 | 优先理解，谨慎修改 |
| `modules/`, `plugins/`, `extensions/` | 功能模块 | 主要开发区域 |
| `scripts/`, `bin/`, `tools/` | 辅助脚本/工具 | 可执行，需加执行权限说明 |
| `tests/`, `__tests__/`, `spec/` | 测试代码 | 修改功能后提醒补测 |
| `docs/`, `documentation/` | 文档 | 更新日志和病历本优先放这里 |
| `backups/`, `archives/` | 历史备份 | **禁止直接修改**，仅用于回滚复制 |
| `logs/`, `temp/`, `runtime/` | 运行时生成 | 错误日志需追加，勿纳入版本控制 |
| `.env`, `.env.*`, `secrets.*` | 敏感配置 | **AI 禁止读取**，仅在部署时提醒检查 |
| `CHANGELOG.md` | 人类可读更新说明 | 每次任务后 **必须** 更新 |
| `AI_DEBUG_HISTORY.md` | AI 调试病历本 | 记录失败尝试，防止重复踩坑 |

> *如果当前项目目录结构与上述约定不符，AI 应主动询问用户：“老板，这个项目好像没按标准目录来，需要我帮你梳理一下吗？”*

## 三、 🩺 调试记忆机制 (核心价值)
**痛点**：跨技术栈开发时，经常忘记“上次试了什么、为什么不行”。

### 1. 错误日志自动记录
- 无论运行什么命令（`python main.py`、`npm start`、`go run .`），只要报错，AI 应**主动**建议将关键错误信息追加到 `logs/error.log`（如目录不存在则先创建）。
- 格式：`[YYYY-MM-DD HH:MM] {{PROJECT_NAME}} 错误：{简化的报错信息}`

### 2. 病历本强制维护 (`AI_DEBUG_HISTORY.md`)
- 这是一个 **专门写给 AI 自己看** 的 Markdown 文件。
- **写入触发条件**：AI 提出的某个修复方案**执行后依然失败**。
- **写入内容模板**：
  ```markdown
  ## [日期] 尝试失败记录
  - **问题描述**：...
  - **尝试的方法**：...
  - **失败现象/报错代码**：...
  - **推测原因**：...
  - **下一步计划**：...
  ```
- **作用**：下次新会话开始时，AI **必须** 先读取此文件。若存在未解决问题，优先基于“下一步计划”继续尝试，避免循环试错。

## 四、 📝 版本更新铁律 (即使只有你一个人开发)
每次 AI 完成一项你交代的任务（改完代码、新增功能、修复 Bug），**必须** 执行以下三步：

1. **更新 `CHANGELOG.md`**：用 **大白话** 在文件最上面记录。
   - 格式：`### [YYYY-MM-DD] - 做了啥`
   - 内容：用非技术语言描述改动，例如“修好了点导出按钮就闪退的毛病”。

2. **口头提醒备份**：AI 应说：“**老板，代码改完了。如果现在功能正常，建议手动备份一下关键文件到 `backups/` 文件夹，防止下次改坏了。**”

3. **版本号建议 (可选)**：若用户不知如何命名版本，AI 按以下逻辑建议：
   - 小修小补 → 第三位 +1 (1.0.1)
   - 加了个新功能 → 第二位 +1 (1.1.0)
   - 大换血 → 第一位 +1 (2.0.0)

## 五、 🤖 AI 行为规范 (跨技术栈通用约束)

### 首次进入项目
1. 扫描根目录下是否存在 `AI_DEBUG_HISTORY.md` 和 `CHANGELOG.md`。
2. 若存在，默读一遍，了解历史背景。
3. 若不存在，主动告知用户：“*这是个新项目，还没有病历本和更新日志。我会帮你建好。*”

### 代码生成与修改
- **注释语言**：一律使用 **中文**，解释“为什么这么写”而不是“写了什么”。
- **错误处理**：任何涉及 I/O、网络请求、外部命令的代码，**必须** 包含 `try...catch` (或语言等价结构)，并给出人类可读的错误提示。
- **环境无关性**：严禁在代码中写死绝对路径（如 `C:\Users\xxx`），使用相对路径或环境变量。

### 任务结束前
- **必须** 询问：“*需要我把刚才的改动记到 `CHANGELOG.md` 里吗？如果有失败的尝试，要记在 `病历本` 里防止下次再犯吗？*”

## 六、 🔄 项目切换与重置
本规则不绑定任何特定项目名称或技术栈。
- 当你开始一个**全新项目**时，只需将本规则文件复制到新项目的 `.codebuddy/rules/` 目录下。
- 第一次对话时告知 AI：“**新项目叫 `某某某`，是个 `某某类型` 的项目。**” AI 即会重置上下文，开始按本规则行事。

---
**规则生效确认**：
*本规则适用于当前项目 `{{PROJECT_NAME}}`。AI 务必遵循以上所有条款。*
```

---

### 💡 使用示范

**场景：你新搞了一个 Go 语言写的 API 服务**

> 你：**“小助理，新项目叫 `mory-api`，是个 Go 语言写的后端接口。规则里那个 `{{PROJECT_NAME}}` 换成这个。”**
>
> AI：**“收到老板。已识别项目 `mory-api`，技术栈 Go。我会按规则管理病历本和更新日志。”**

然后你就可以随便开发了，AI 会自动记住调试历史，不会用 Python 的思维去写 Go 代码。

---

### 🎯 为什么这份规则适合你？

| 你的特点 | 规则如何应对 |
| :--- | :--- |
| 技术栈跨度大 | 不写死语言，让 AI 自动推断 |
| 调试环境不固定 | 强制维护“病历本”，跨会话记忆 |
| 小白友好 | 更新日志用大白话，AI 主动提醒备份 |
| 换项目频繁 | 规则通用，复制粘贴即用 |