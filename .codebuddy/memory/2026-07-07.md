# 2026-07-07

## 项目深度审计（mory_assistant）
- 按用户粘贴的《AI Project Audit Charter v2.0》对 mory_assistant 执行只读审计，产出 `PROJECT_AUDIT_REPORT.md`（根目录 + 桌面副本）。
- 核心结论（重要，推翻章程预设的"虚报实现"病灶）：**功能真实非虚报**。核心能力（人设对话/广告5层/群管/转化漏斗/新闻播报/Dashboard/RBAC）均实测存在。
- 主要真实病灶：
  1. 文档数字夸大与自相矛盾：模块数 README 写 87+48 又写 95+35（实测 modules 92 / core 73）；_job_ 写 52 又写 53（实测 53）；"34 拦截点"实为 9 个分发函数；"25 mode 路由"实为 11 项映射；cart_recovery 文档称"每小时"实则每5分钟。
  2. 文件卫生差：backup/ + backups/ 两套共 22 个 .bak；根目录散落 fault_dedup_state.json / reload_flag / test_selfcheck.db / _ssh_known_hosts（勿删）。
  3. 文档膨胀：AI_DEBUG_HISTORY.md 4440 行、CHANGELOG 2456 行；AGENTS.md 仅 184 行尚精简。
  4. 死代码：structured_logger.get_struct_logger/clear_context、pinyin_util.has_pinyin_leak 无调用。
- 安全健康：密钥 gitignore、RBAC before_request 默认拒绝、无硬编码用户密钥（仅 scan_group.py 硬编码 Telegram 公开 api_hash 且已 gitignore）。无 P0 阻断项。
- 复核要点：子代理曾误报 `_dispatch_p10_ai`"未定义会崩溃"，实际定义于 ai_reply_handler.py:73 并被导入，生产正常——主审计员已纠正。
- 未覆盖：未逐模块精读（抽样）、未执行 pytest/verify_db_methods（避副作用），相关"测试通过"声明标 [Unknown]。

## 整改执行（全流程合并任务书 v3.0 · Part 3 执行者）
- 按任务书身份路由（IDE 编码代理=执行者）执行 Part 3，仅做 P0+P1+清理/迁移+文档治理，P2 留下一轮。
- Phase 0 基线 `e20abf1` → Phase 3+4 `a658f2a`（隔离 9 项垃圾到 `_quarantine_20260707/`、删除根 `__pycache__`/`.pytest_cache__`、保留 `_ssh_known_hosts`、新增 `scripts/doc_consistency.py`、归档 6 旧文档到 `docs/archive/20260707/`、迁移审计报告到 `runtime/audit-reports/`）→ Phase 5 `48d2c91`（重建 6 根文档，行数 8593→289 全部达标，写入防失真 METRICS 块）→ Phase 6 `a54659e`（验收报告 `EXECUTION_REPORT.md` + 执行台账 `EXECUTION_LOG.md`）。
- 实测关键：modules_py=91（审计误报 92）、model_router_mappings=10（审计误报 11）；core_py=73、_job_=53、db_tables=108、dashboard_routes=157、dispatch_funcs=9。
- `python scripts/doc_consistency.py` 退出码 0，可作 CI/提交前门禁。
- 下一轮待办（P2）：ISSUE-002 死代码、ISSUE-003 合并重复 handler 路由、ISSUE-005/006/007 文档数字修正、ISSUE-010/011/012。
- P2 收尾（同夜）：8 项 P2 全部完成，commit `5a46603` 已 push 到 origin/main。`core/handlers/utility_dispatch.py` 抽取两 handler 重复工具命令分发（19 分支）；删 `structured_logger.get_struct_logger/clear_context`、`pinyin_util.has_pinyin_leak` 死代码；`capability-matrix.md` 将 cart_recovery「每小时」→「每5分钟」；`profile_learner` 以 `STICKER_DIMENSION_ENABLED=False` 显式标注 sticker 维度未启用；`scan_group.py` 硬编码 api_hash 常量化（gitignore 不进提交）；`docs/plans/remediation_roadmap.md` 补齐计划文档。doc_consistency 门禁在 P2 中即时拦出新增文件导致 core_py 73→74 并修正。ISSUE-005/006 经核实已消解/误报（README 无 34 拦截点；config MODE_ROUTING 实测 25，文档"25 mode"准确）。VPS 生产部署未自动执行（避免离开时无人可查的生产中断）。
- **VPS 生产部署已执行（用户硬性要求）**：`python deploy_vps.py` 成功（326 文件上传、依赖 pip check 通过、config 安全合并、双系统服务启动）。独立上机验收：`mory-assistant`+`mory-dashboard` 均 active、health `200`、新增 `utility_dispatch` 服务端导入 OK（UTIL_IMPORT_OK）、定时任务 executed successfully、NRestarts 全 0、日志无 ImportError/Traceback。配置 ALL_CONFIG_OK。
- **部署发现（非阻塞缺口）**：`mory-media-assistant`/`mory-media-dashboard` 处于 inactive。根因是 `config/mory-media-assistant.service` 的 ExecStart=`main.py --media`，但 `main.py` 从未定义 `--media` 参数（全仓无 argparse）——属指向不存在功能的陈旧桩服务。部署脚本权威做法也只拉起 assistant+dashboard。结论：当前生产态=双核心服务，media 桩不应启动（强行启动会 unrecognized arguments 反复崩溃）。列为待办，不擅自实现 --media 模式（超出范围且风险高）。
- **用户质疑 media 服务是否"另一个项目同名"——复核结论：不是同名撞车**。上机核实：`/etc/systemd/system/mory-media-*.service` 内容 `WorkingDirectory=/home/ubuntu/mory_assistant`、`main.py --media`，由本项目的 `deploy_vps.py` 的 SERVICE_FILES 上传（脚本只 start/enable 双核心，media 两个仅上传不启动→disabled）。VPS 上另一媒体项目是 `mediaops-coo`，其 systemd 服务名为 `mediaops-openai-api.service`（完全不同命名），无冲突。故 media 两服务确属 mory_assistant 自身的陈旧桩，建议从 `config/` 及 VPS 删除（待用户确认）。
- **深度复核 media 模式真实可用性（用户要求"调研好、判断好"）**：main.py 无任何参数解析（无 argparse、无 `--media`）；`mode=="media"` 在 dashboard/auto_tasks 里走的是环境变量 `DASHBOARD_MODE=media`，非 CLI flag。故 `mory-media-assistant.service` 的 `main.py --media` 写法错误、启动必崩。VPS 实机：`mory-media-*` 均 inactive；`mory_media.db` 不存在且无进程打开（`NOT_OPEN`）；`/home/ubuntu` 下无 `mory_media_assistant` 目录；真正在跑的媒体/宣发 bot 是独立项目 `/opt/moryfansbot`（bot.py+web_admin），不依赖本仓库 media 单元。结论：本仓库 media 两 unit 是孤立坏桩，谁都不依赖。
- **已执行清理（用户拍板"只搞一个并且搞稳定"）**：删除本地 `config/mory-media-assistant.service`、`config/mory-media-dashboard.service`；从 `deploy_vps.py` 的 SERVICE_FILES 移除这两项（部署脚本有 `if local_svc.exists()` 守卫，删文件不会让后续部署报错）；VPS 上 `systemctl disable`+`rm -f /etc/systemd/system/mory-media-*.service`+`daemon-reload`。验证：`list-unit-files | grep mory-media` → NONE_LEFT；`mory-assistant`/`mory-dashboard` 仍 active；health 200。仓库临时脚本/日志已清，未入库。现线上仅一套稳定部署（双核心服务）。
- **残留说明（非阻塞，未动）**：代码仓内仍有 media 模式 env 驱动代码（dashboard DASHBOARD_MODE=media、auto_tasks `_mode=="media"`→mory_media.db、core/shared_db.py 跨 Bot 共享库），均为休眠分支，仅在设 env 时生效，删除 unit 不影响；docs/archive 与 docs/technical/vps-deploy-trap.md 仍提及 `mory_media_assistant` 进程（指 /opt/moryfansbot），属历史描述，非阻断。
- **【架构级事实·跨会话】mory_assistant 与媒体/宣发 bot 是 split-brain**：真媒体 bot 在 VPS `/opt/moryfansbot`（bot.py+web_admin），**不在本仓库**，且**共用生产库 mory.db 的 `promotions` 表**（archive 快照:410 明文）。文档把宣发号写成 `mory_media_assistant`，但 VPS 上该路径不存在、真身是 /opt/moryfansbot——即 AGENTS 体系里关于 media bot 的描述是过期错位。决策含义：媒体需求第一反应去 /opt/moryfansbot；改 mory.db 写操作要评估对宣发 bot 的锁/长事务影响；文档里 `mory_media_assistant` 提法当过期处理。
- **【风险·未提交】media 清理未入库**：git status 显示 ` D config/mory-media-*.service`、` M deploy_vps.py` 均为未暂存。若本地 restore/discard,deploy_vps.py 会重带坏桩、下次部署又把 mory-media-* 传回 VPS。须 commit(+push) 才安全。AGENTS.md 铁律：未经用户明确要求不主动 commit。
