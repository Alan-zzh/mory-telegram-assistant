# 📝 CHANGELOG · Mory 更新日志

---

## 2026-04-20 | v4.3.1 | 稳定性优化

### 紧急修复

**API_KEY 配置冲突**
- `ai_engine.py` 的 `analyze_image` 函数原读取 `DASHSCOPE_KEY`，但 `main.py` 初始化时映射到 `API_KEY`
- 修复：统一读取 `API_KEY`，兼容旧 `DASHSCOPE_KEY` 字段

**线程安全加固**
- `main.py` 的视奸雷达冷却字典 `_radar_cooldown` 在多线程环境下未加锁
- 修复：添加 `_radar_lock` 互斥锁，防止并发修改导致 RuntimeError

### 代码清理
- 删除 `ai_engine.py` 中重复定义的 `_is_model_expired` 调用链（保留带过期检查的版本）

### 提示优化
- 启动版本显示自动同步 `config.json` 的版本号
- 优先读取 `_CONFIG_VERSION`，兼容 `VERSION` 字段

---

## 2026-04-20 | v4.3.0 | 功能大版本更新

### 🆕 新增功能

**Docker一键部署**
- 新增 `Dockerfile`：Python 3.11-slim + 所有系统依赖
- 新增 `docker-compose.yml`：一键启动，自动重启，日志轮转
- 新增 `docker_deploy.sh`：start/stop/restart/logs/update 傻瓜脚本

**AI识图互动**
- 用户（非管理员）发图片 → AI识别内容 + 撩人评论
- 管理员发图片 → 原有打码推群功能不变
- 随机概率触发（默认30%），节省token
- 配置项：`ENABLE_VISION_REPLY` / `VISION_REPLY_RATE`

**活跃勋章系统**
- 新增10种勋章：早起鸟🐦 / 夜猫子🦉 / 社牛🦋 / 话痨💬 / 铁粉❤️ / 超级铁粉💖 / OG会员👑 / 寻宝达人💎 / 塔罗师🔮 / 幸运星⭐
- 自动检测并授予勋章
- 命令「我的勋章」查看全部

**热更新配置**
- 管理员发送「热更新」或「reload」无需重启程序
- 重新加载 config.json，保留动态状态

### 优化调整
- 图片处理重构：管理员打码 + 用户识图分离
- 数据库添加 replied 索引（上一版本）
- 塔罗解析正则增强（上一版本）

---

## 2026-04-20 | v4.2.8 | 架构收敛与稳定性补丁

### 紧急修复
- **模型过期危机**：`_is_model_expired()` 自动检查，到期模型加入黑名单并切换
- **数据库索引增强**：添加 `idx_track_replied(ts, replied)` 复合索引，加速孤儿消息查询防止全表扫描

### 健壮性增强
- **塔罗解析重写**：从简单关键词匹配升级为正则表达式精准捕获
  - `牌面描述`：正则匹配 + 容错兜底
  - `今日解读`：正则匹配 + 最长句子兜底
  - `幸运色/方位/数字/星座/时段`：正则+全文搜索双保险
  - 即使AI输出格式混乱也能正确解析

---

## 2026-04-20 | v4.2.8 | AI引擎模型过期自动跳过

### 修复问题
- **模型过期逻辑缺陷**：添加`_is_model_expired()`检查，到期日期的模型自动跳过并加入黑名单
- `_ensure_valid_model()`同时检查黑名单和过期状态
- `_next_available_model()`遍历时跳过过期模型

---

## 2026-04-20 | v4.2.7 | 全自动任务文案AI升级

### 优化调整
- **叫醒服务**：固定文案 → AI生成个性化叫醒语（每天每用户不同）
- **醋意挽回**：固定文案 → AI生成撩人召回语（每天固定）
- **购物车挽回**：固定文案 → AI生成隐晦引导语（不直接提门槛/价格）
- 所有AI文案失败时自动回退到备用方案

---

## 2026-04-20 | v4.2.6 | 塔罗运势AI生成升级

### 优化调整
- **全部内容改为AI生成**：牌面解读、建议、幸运元素等全部由AI实时生成，不再是预设模板
- 牌名+主题保留预设随机（保持神秘感+稳定seed）
- AI生成失败时自动回退到备用方案
- 同一人同一天结果仍然固定（用户ID做seed）

---

## 2026-04-20 | v4.2.5 | 每日塔罗搭讪（增强版）

### 新增功能
- 每天下午3点随机30%概率触发
- **主动@目标用户**，让他知道在跟他说话
- **HTML富文本卡片**，群内显示超美观
- **超详细塔罗运势模板**：
  - 牌名+正逆位+牌面解读
  - 今日建议
  - 开运颜色/幸运方位/幸运数字/贵人星座
  - 幸运时段/宜/忌

### 优化调整
- **同一人同一天结果固定**：用用户ID做seed缓存，北京时间(UTC+8)为准
- **卡片式排版**：层次分明，有段落感
- **高度随机**：40%短版(~90字)，60%长版(~200字)
- **隐晦撩人转化文案**：不提会员/付费/订阅，暗示有专属内容让人心动
  - 8种主题详细解读
- **AI生成个性化转化结尾**，永远不重复

---

## 2026-04-20 | v4.2.4 | 每日私聊数据报告

### 新增功能
- 每天早上9:10自动发送私聊报告给管理员
- 富文本HTML格式，手机屏幕完美显示
- 包含群动态（入群/离群/净增）+ 内容表现（浏览量统计）
- 带昨日对比趋势箭头，一目了然

---

## 2026-04-20 | v4.2.3 | 数据监控全面升级

### 画像标签扩展
- 18种行为标签：内容偏好、付费意向、互动偏好、情感状态
- 精准匹配关键词，自动打标签

### 群数据监控
- 入群/离群统计自动记录
- 本周净增人数看板显示
- 历史数据可追溯

### 频道内容追踪
- 机器人发的群消息自动记录
- 浏览量每小时更新
- 内容表现数据统计

### 管理员通知升级
- 显示完整用户消息和AI回复
- 用户名可点击直达私聊窗口

---

## 2026-04-19 | v4.2.0 | 项目全面精简

### 清理内容
- 归档 111 个调试脚本和文档到 `_archive_scripts/`
- 删除 `docs/`、`backup_deploy/`、`scripts/` 目录
- 删除所有 `.pyc` 编译缓存
- 精简根目录批处理文件
- 创建 `.gitignore` 忽略运行时文件

### 驳回错误的"架构重构"建议
- pyTelegramBotAPI handler 独占机制，`return False` 不生效
- 使用 `BaseMiddleware` 是唯一正确方案

---

## 2026-04-19 | v4.1.0 | 架构级修复

### 核心修复
- `ReplySnifferMiddleware` 中间件解决"机器人眼瞎"问题
- 清理重复嗅探逻辑
- APScheduler Cron 语法修正

---

## 早期版本

详见 `_archive_scripts/docs/CHANGELOG.md`（归档）
