# 更新日志

## 2026-04-30

- 文档：明确生产环境只允许 `systemd` 管理 `mory_assistant`（禁止 pm2 / `bash start.sh start` / 手动 `python main.py`），避免 Telegram `409 Conflict`（同 token 多开 getUpdates）。
- 文档：补充 `BOT_ROLE`（后台任务防冲突判断）与同机 `mory_media_assistant` 读取 `promotions` 表的协作边界说明。

## v4.5.15 | 2026-04-29

- 接通 Telegram 里的自然语言配置入口，管理员现在可以直接和机器人说中文改设置。
- 网页后台“自然语言配置”改为走后端统一解析，不再只靠前端正则硬猜。
- 新增特定词自动回复的自然语言维护能力：支持查看、新增、修改、开启、关闭、删除。
- 部署前会先把服务器当前投喂过的运行时配置拉回本地，再继续同步，避免线上内容被旧本地配置覆盖。
- 新增 [BOT_投喂与自然语言配置说明.md](BOT_投喂与自然语言配置说明.md) 说明文档。

## v4.5.14 | 2026-04-28

- 修复 `SPECIAL_AUTO_REPLIES` 在部署安全合并时被遗漏的问题。
- 补齐 VPS 配置同步白名单，确保特定词自动回复规则可以随版本一起下发到服务器。
- 增加这类配置上线后的远端实值校验，避免出现“本地有、线上没生效”的假同步。

## v4.5.13 | 2026-04-28

- 系统人设支持把 `老板`、`boss`、`Mory` 当作同一人，回复时按语境自然切换称呼。
- 新增 `SPECIAL_AUTO_REPLIES` 配置板块，支持特定词数组自动触发回复。
- 特定词回复支持先走模板，再用 AI 润色成更自然的人话。
- 预置了价格、开通、完整版、联系Mory 四类默认引导规则。

## v4.5.12 | 2026-04-28

- 强化早安、午安、晚安提示词的随机性，要求每次场景、句式、节奏都明显不同。
- 新增隐晦高情商转化牵引：自然引导联系你、靠近更深圈层、想看更完整内容。
- 明确禁止直白营销词，但保留对完整版、至臻精选、至臻全享、私聊和偏爱的轻暗示。

## v4.5.11 | 2026-04-28

- 取消早中晚重复新闻播报，统一为每个时段只发一条主新闻。
- TrendRadar 改为优先新闻源，不再直接把原始标题列表发到群里。
- 新闻去重改成发送成功后再记录，避免发送失败却被误判成“今天发过”。
- 早安、午安、晚安提示词改回自然真人口吻，去掉过重广告腔。
- 新闻播报提示词改为自然转述，不再使用固定主持腔收尾。

## v4.5.10 | 2026-04-28

- 修正模型策略：全模态模型可处理文本时，主聊天优先使用全模态模型。
- 三层智能路由每个档位都先尝试omni池，失败后再退回对应llm档位。
- 保留按本轮实际调用模型做熔断、日志、慢速避让，避免模型指针跑偏。
- 修复服务器启动脚本版本号写死问题，启动横幅自动读取统一版本文件。

## v4.5.9 | 2026-04-28

- 加固主业务AI模型切换：熔断检查改为针对本轮实际调用模型。
- 修复当前模型指针偶尔指到omni池的问题。
- 独立路由模块移除硬编码API密钥，改为从`.env`读取。
- 修复账号失败策略：普通失败累计到阈值才禁用，429限流只冷却5分钟。
- 修复`router_config.json`被部署工具误跳过的问题。
- 移除独立路由里的过期模型，服务器测试通过。

## v4.5.8 | 2026-04-28

- 修复 Windows BAT 脚本含中文导致 CMD 乱码闪退的风险。
- 移除一键部署脚本中的 `python -c`，改为调用独立预检脚本。
- 新增 `check_vps_local.py`，部署前检查 VPS 配置是否完整，但不输出密码。
- 新增 `start_dashboard.py`，Dashboard 启动时从 `.env` 读取密钥；缺失时生成本次临时密码。
- 修复 `sync_vps.py` 导入部署脚本时可能误触发部署的问题。
- 收窄多处裸 `except`，避免吞掉系统退出信号。
- 完成本地语法检查和 VPS 只读状态检查：线上 Bot 正常运行，但版本仍落后于本地。
- 新增 `windows_helper.py`，把中文提示放到 Python 里，BAT 只做纯英文启动壳。
- 重建 VPS 部署链路：部署前自动拉回服务器数据库/日志/配置备份，停止旧进程后再上传，避免旧配置覆盖新配置。
- 部署改为同步完整运行文件，修复线上长期停留在旧版本的问题。
- 修复模型池缩短后 `CURRENT_MODEL_INDEX` 越界导致启动失败的问题。
- 补齐服务器依赖安装规则，并给主要依赖增加版本上限。
## 2026-04-29

- 新增服务器重装后的本地可信数据一键恢复说明：`RECOVERY_AFTER_REINSTALL.md`。
- 新增恢复配置模板：`restore_config.example.json`。
- 新增恢复脚本：`scripts/restore_after_reinstall.py`，支持打包、上传、解压、启动、验证和健康检查。
- 新增 Windows PowerShell `8009001d` 启动失败修复脚本：`scripts/repair_powershell_8009001d.cmd`。
- 完成腾讯云硅谷二区 Ubuntu 24.04 VPS 恢复部署：机器人与 Dashboard 均已设置 systemd 自启动。
- 完成 VPS SSH 密码轮换后的本地与服务器 `.env` 同步，并重启验证服务状态。
- 统一本项目 Telegram Bot Token：本地和服务器 active 配置均只保留 `@MoryMateBot` 对应 token，并补充多项目服务器操作边界。
